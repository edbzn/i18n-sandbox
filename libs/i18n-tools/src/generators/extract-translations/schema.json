{
  "$schema": "https://json-schema.org/schema",
  "$id": "ExtractTranslations",
  "title": "Extract translations from React source files",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to extract translations from",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Which project would you like to extract translations from?"
    },
    "outputPath": {
      "type": "string",
      "description": "Path where the extracted translations JSON file will be saved",
      "default": "i18n/translations.json"
    },
    "pattern": {
      "type": "string",
      "description": "Glob pattern to match translation function calls (e.g., t('key'), i18n.t('key'))",
      "default": "t\\(['\"`]([^'\"`]+)['\"`]\\)"
    }
  },
  "required": ["project"]
}
